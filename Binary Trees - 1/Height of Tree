def height(root) :
	#Your code goes here
    if root==None:
        return 0
    if root.right!=None or root.left!=None:
        return 1+max(height(root.left), height(root.right))
    else:
        return 1
